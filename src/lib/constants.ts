/** Invitación al servidor de Discord de la comunidad. */
export const DISCORD_URL = "https://discord.gg/HFV4QXJKNh";

/** Validación básica de email (formato, no existencia). */
export const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
